{
  "name": "pta-personal-trading-assistant",
  "version": "1.0.0",
  "description": "Real-time event-driven market intelligence engine for intraday options trading",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:mock": "USE_MOCK=true node server.js",
    "dev": "nodemon server.js",
    "build:engine": "tsc -p engine",
    "build": "npm run build:engine && npm --prefix frontend run build",
    "production": "npm run build && npm start",
    "test": "npm run test:engine && npm run test:server",
    "test:engine": "npm run build:engine && node --test engine/test",
    "test:server": "node --test test"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.5.0",
    "express": "^4.19.0",
    "pg": "^8.22.0",
    "redis": "^4.6.0",
    "totp-generator": "^1.0.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "form-data": "4.0.6"
  },
  "engines": {
    "node": ">=24.0.0 <25"
  }
}
